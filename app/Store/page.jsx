import React from 'react';
import ProductList from '../components/ProductList';

const Page = () => {
    return (
        <section>
            <ProductList/>
        </section>
    );
};

export default Page;